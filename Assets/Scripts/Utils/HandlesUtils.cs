using UnityEngine;

namespace Utils
{
    public class HandlesUtils : MonoBehaviour
    {
        public static readonly Color TranspanentRed = new Color(1f, 0f, 0f, 0.1f);
        public static readonly Color TranspanentGreen = new Color(0f, 1f, 0f, 0.1f);
    }
}
